name: bump zenv

on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  # Split from `bump` so that the hourly no-op run only resolves actions/checkout.
  # A single job would make "Set up job" download create-pull-request every hour,
  # which is what got rate limited (HTTP 429) by codeload.
  check:
    runs-on: ubuntu-latest
    outputs:
      changed: ${{ steps.check.outputs.changed }}
      version: ${{ steps.check.outputs.version }}
      tag: ${{ steps.check.outputs.tag }}
    steps:
      - name: Check out tap
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1

      - name: Compare formula version with latest zenv release
        id: check
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail

          repo="m-mizutani/zenv"
          formula="Formula/zenv.rb"

          latest_tag=$(gh release view --repo "$repo" --json tagName --jq .tagName)
          latest_version=${latest_tag#v}
          # The formula carries no `version` field: brew audit rejects it as
          # redundant with the version scanned from the release URL.
          current_version=$(sed -n 's|.*/releases/download/v\([^/]*\)/.*|\1|p' "$formula" | head -n 1)

          if [ -z "$current_version" ]; then
            echo "Could not read the current version from $formula" >&2
            exit 1
          fi

          echo "current=$current_version latest=$latest_version"
          if [ "$current_version" = "$latest_version" ]; then
            echo "Already up to date."
            echo "changed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          {
            echo "changed=true"
            echo "version=$latest_version"
            echo "tag=$latest_tag"
          } >> "$GITHUB_OUTPUT"

  bump:
    needs: check
    if: needs.check.outputs.changed == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Check out tap
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1

      - name: Update formula to latest zenv release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          LATEST_TAG: ${{ needs.check.outputs.tag }}
          LATEST_VERSION: ${{ needs.check.outputs.version }}
        run: |
          set -euo pipefail

          repo="m-mizutani/zenv"
          formula="Formula/zenv.rb"
          latest_tag="$LATEST_TAG"
          latest_version="$LATEST_VERSION"

          declare -A sha
          for platform in darwin-amd64 darwin-arm64 linux-amd64 linux-arm64; do
            asset="zenv-${latest_tag}-${platform}.tar.gz"
            gh release download "$latest_tag" --repo "$repo" --pattern "$asset" --output "$asset"
            sha[$platform]=$(sha256sum "$asset" | cut -d' ' -f1)
            rm -f "$asset"
          done

          cat > "$formula" <<EOF
          class Zenv < Formula
            desc "Enhanced env command to manage environment variables in CLI"
            homepage "https://github.com/m-mizutani/zenv"
            license "Apache-2.0"

            livecheck do
              url :stable
              strategy :github_latest
            end

            on_macos do
              on_arm do
                url "https://github.com/m-mizutani/zenv/releases/download/v${latest_version}/zenv-v${latest_version}-darwin-arm64.tar.gz"
                sha256 "${sha[darwin-arm64]}"
              end
              on_intel do
                url "https://github.com/m-mizutani/zenv/releases/download/v${latest_version}/zenv-v${latest_version}-darwin-amd64.tar.gz"
                sha256 "${sha[darwin-amd64]}"
              end
            end

            on_linux do
              on_arm do
                url "https://github.com/m-mizutani/zenv/releases/download/v${latest_version}/zenv-v${latest_version}-linux-arm64.tar.gz"
                sha256 "${sha[linux-arm64]}"
              end
              on_intel do
                url "https://github.com/m-mizutani/zenv/releases/download/v${latest_version}/zenv-v${latest_version}-linux-amd64.tar.gz"
                sha256 "${sha[linux-amd64]}"
              end
            end

            def install
              bin.install "zenv"
            end

            test do
              output = shell_output("#{bin}/zenv FOO=bar env")
              assert_match "FOO=bar", output
            end
          end
          EOF

      - name: Create pull request
        uses: peter-evans/create-pull-request@5f6978faf089d4d20b00c7766989d076bb2fc7f1 # v8.1.1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: bump-zenv-${{ needs.check.outputs.version }}
          commit-message: "zenv ${{ needs.check.outputs.version }}"
          title: "zenv ${{ needs.check.outputs.version }}"
          body: |
            Automated update of `zenv` to ${{ needs.check.outputs.version }}.

            Updated the release URLs and per-platform `sha256` from the
            upstream release assets at
            https://github.com/m-mizutani/zenv/releases/tag/v${{ needs.check.outputs.version }}.
          delete-branch: true
